import { DATA_GROUP_CONTACT } from '../__data__'

export const INITIAL_GROUP_CONTACTS = DATA_GROUP_CONTACT

export function groupContactsReducer(state = INITIAL_GROUP_CONTACTS, action) {
  return state
}
